# ── Server-only dependencies (CPU inference; no GPU stack) ───────────────────
# Installed in the Dockerfile with:
#   pip install -r api/requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# so `torch` resolves to the small CPU wheel.

# web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # multipart forms (/search/image upload)
anyio>=4.4                     # thread offloading for the sync engine

# config / schemas
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0

# auth
pyjwt>=2.8
argon2-cffi>=23.1
slowapi>=0.1.9                 # rate limiting

# aws (allowed in api/** only — ML logic never imports boto3)
boto3>=1.34

# retrieval runtime (query-side embedding + BLIP-ITM rerank, CPU)
numpy>=1.26
faiss-cpu>=1.8
pillow>=10.3
torch>=2.3                     # CPU wheel via --extra-index-url (see Dockerfile)
transformers>=4.44
# NOTE: open_clip_torch is intentionally NOT listed — it is only required by the
# optional marqo-fashionSigLIP garment backend. The default serving path
# (fashion-clip + clip-vit-b32 + blip-itm) is pure `transformers`.

# tests
httpx>=0.27                    # FastAPI TestClient transport
